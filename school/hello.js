function add1(x, y) {
    return x + y;
}

const add2 = (x, y) => x + y;

const add3 = (x,y) => {
    return x+y;
}

console.log(add1(2, 3)); // 5
console.log(add2(4, 5)); // 9
console.log(add3(6, 7)); // 13